# render-engine
A simple Python-based rendering engine running in Docker that automatically processes and combines one or more input videos into polished outputs based on instruction lists (EDL files).
